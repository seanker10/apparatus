# === Secrets — never commit the real .env to git ===
# Fill these in locally. Do not paste values into chat or anywhere outside this file.

ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=                          # Get from @BotFather. Regenerate via /revoke if ever exposed.
TELEGRAM_ALLOWED_USER_ID=8170693789          # Sean's Telegram user ID — only messages from this ID are accepted
GITHUB_TOKEN=                                # Personal access token with repo scope for seanker10/apparatus
GITHUB_REPO=seanker10/apparatus
GITHUB_BRANCH=main
VERCEL_DEPLOY_HOOK_URL=                      # Optional — set after Vercel project is created
ANTHROPIC_MODEL=claude-opus-4-7
